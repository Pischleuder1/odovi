services:
  odovi-db:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: odovi
      POSTGRES_PASSWORD: odovi
      POSTGRES_DB: odovi
    volumes:
      - odovi-dev-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U odovi"]
      interval: 5s
      timeout: 5s
      retries: 5

  teslamate-db:
    image: postgres:17-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: teslamate
      POSTGRES_PASSWORD: teslamate
      POSTGRES_DB: teslamate
    volumes:
      - teslamate-dev-db:/var/lib/postgresql/data
      - ./dev/fixtures/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U teslamate"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  odovi-dev-db:
  teslamate-dev-db:
